{
  "name": "eios-ls",
  "version": "0.1.0",
  "private": true,
  "description": "Plataforma web institucional de Labor Social / Compromiso Social UP con asistente de IA.",
  "workspaces": [
    "backend",
    "frontend",
    "chatbot"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "build": "npm run build --workspace=chatbot && npm run build --workspace=backend && npm run build --workspace=frontend",
    "lint": "npm run lint --workspace=backend && npm run lint --workspace=frontend",
    "format": "npm run format --workspace=backend && npm run format --workspace=frontend",
    "test": "npm run test --workspace=backend"
  }
}
